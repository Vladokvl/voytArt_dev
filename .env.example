AUTH_SECRET=""
AUTH_TRUST_HOST="true"

# Public site URL (використовується в metadata, sitemap, robots)
NEXT_PUBLIC_APP_URL=""

# Prisma
DATABASE_PASS=""
# Connect to Supabase via connection pooling
DATABASE_URL=""
# Direct connection to the database. Used for migrations
DIRECT_URL=""

# Cloudinary — https://console.cloudinary.com/
CLOUDINARY_CLOUD_NAME=""
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# Початковий адмін-користувач для `npm run db:seed:admin` (необовʼязково;
# якщо порожньо — скрипт використає значення за замовчуванням і згенерує пароль)
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
